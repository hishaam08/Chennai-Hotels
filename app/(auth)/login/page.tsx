import Login from "@/components/auth/Login";
import React from "react";

export const metadata = {
  title: "Login - Chennai Hotels",
};

const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;
